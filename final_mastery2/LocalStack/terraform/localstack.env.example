LOCALSTACK_API_KEY=ls-your-localstack-pro-key

# 使用方式：
#   cp localstack.env.example localstack.env
#   编辑 localstack.env 填入真实的 API Key。
#
# 启动 LocalStack 前可执行：
#   set -a
#   source localstack.env
#   set +a
#   localstack start

